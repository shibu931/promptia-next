export const metadata = {
    title: "Create Prompt",
    description: "create prompt page"
}

const RootLayout = ({ children }) => {
    return (
        <>
            {children}
        </>
    )
}

export default RootLayout